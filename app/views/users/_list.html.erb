<table class='table'>
  <tr><%= attachment_image_tag(user, :profile_image, :fill, 50, 50, class:"mt-3 rounded-circle", fallback: "//"+request.host+asset_path("noimage.png"), size:"100x100") %></tr>
  <tr>
    <th>name</th>
    <th><%= user.name %></th>
  </tr>
  <tr>
     <tr>
     <th>follows</th>
     <th><%= link_to user.followings.count, user_followings_path(user), class: "user_#{user.id}" %></th>
  </tr>
  <tr>
     <th>followers</th>
     <th><%= link_to user.followers.count, user_followers_path(user), class: "user_#{user.id}" %></th>
  </tr>
  <tr>
    <th>自己紹介文</th>
    <th><%= user.introduction %></th>
  </tr>

  <tr>
    <th><%= link_to '',edit_user_path(user),class: "btn btn-outline-secondary btn-block fas fa-user-cog edit_user_#{user.id}" %></th>
  </tr>

</table>
